title:            scribble-cn
description:      # Site description which will show in site meta data
url:              # Site url, without a slash at the end.
signoff:          # Your name.
disqus_shortname: # Shortname of disqus for comments in post. You can only set one between disqus_shortname and duoshuo id.
duoshuo_id:       # ID of duoshuo for comments in post. You can only set one between disqus_shortname and duoshuo id.
facebook_appid:   # appid for like button, remove from /_includes/share.html if unwanted.
google_analytics: # Set tracking, remove from /javascripts/basic.js if unwanted.

links:
  - name:         Home
    url:          /
  - name:         About
    url:          /about
  - name:         GitHub
    url:          http://github.com/jxltom
    external:     true
permalink:        pretty

# Jekyll 3 now only supports Kramdown for Markdown
kramdown:
  # Use GitHub flavored markdown, including triple backtick fenced code blocks
  input: GFM
  # Jekyll 3 and GitHub Pages now only support rouge for syntax highlighting
  syntax_highlighter: rouge
  syntax_highlighter_opts:
    # Use existing pygments syntax highlighting css
    css_class: 'highlight'
